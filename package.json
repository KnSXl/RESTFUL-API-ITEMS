{
    "scripts": {
        "serve": "cd BACKEND && php artisan serve",
        "up": "cd DB && docker-compose up",
        "all": "npm-run-all --parallel up serve"
    },
    "dependencies": {
        "npm-run-all": "^4.1.5"
    }
}